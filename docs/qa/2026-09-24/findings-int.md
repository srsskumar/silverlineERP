# Findings ledger — Integrations (Task 6)

Live checks against the deployed VM (`dev-thor`) for every integration in `docs/PROVIDERS.md`
plus `apps/api/src/modules/jobs` and a web/mobile record-consistency spot check. Companion to
`docs/qa/2026-09-24/coverage.md`. Probe scripts used are committed under
`scripts/qa/integrations/` and were run from `~/sl-e2e/integrations/` on the VM using the
existing QA tokens/dataset in `~/sl-e2e/` (no secrets read, no non-QA data touched).

| ID | Sev(P0–P3/DECISION/KNOWN-TLS) | Surface(web/mobile/api) | Module | Steps | Expected | Actual | Status | Fix commit |
|---|---|---|---|---|---|---|---|---|
| I-001 | — | api | uploads / ClamAV | Re-verified the 2026-09-22 scanner setup live: `POST /api/v1/tasks/:id/evidence` with a clean `%PDF-1.4` file, then with a file crafted to contain the EICAR test string. First attempt embedded EICAR by simply appending it after a fake `%PDF-` header — that upload came back **201**, not 422. Direct `INSTREAM` tests against ClamAV (127.0.0.1:3310) on the VM showed why: ClamAV's `Eicar-Test-Signature`/`Eicar-Signature` detection is anchored to the start of whatever unit it scans — one stray byte before or after the 68-byte EICAR string, even with no PDF wrapper at all, drops detection to "OK". Rebuilt the probe with a minimal, well-formed PDF carrying EICAR as a self-contained `/EmbeddedFile` stream object (`scripts/qa/integrations/build-eicar-pdf.mjs`), so ClamAV's PDF parser extracts and scans that stream as its own unit (offset 0 = EICAR). | Clean upload → 201; EICAR-in-PDF → 422 `UNSAFE_FILE` | Clean upload → 201 (checksum recorded). Properly-embedded EICAR PDF → 422 `{"code":"UNSAFE_FILE","message":"This file failed the malware scan"}`. ClamAV container `silverline-clamav` healthy, 32h+ uptime, 3,628,071 signatures loaded, `SelfCheck: Database status OK` every ~10 min in its logs. Scanner re-verified working correctly. The only "surprise" is a property of ClamAV/EICAR itself (offset-anchored signature), not a Silverline defect — noted here so a future tester doesn't waste time on a naive EICAR file and misdiagnose the scanner as broken. | VERIFIED — NOT A BUG (probe-construction note only) | — |
| I-002 | — | api | integrations (SMS/WhatsApp/Weather/Accounting) | `GET /api/v1/integrations` as `qa-admin-superadmin` | Per `docs/PROVIDERS.md`: "SMS, WhatsApp, weather, and accounting have no vendor selected and are disabled by default" | `{"data":[{"id":"SMS","status":"DISABLED"},{"id":"WHATSAPP","status":"DISABLED"},{"id":"WEATHER","status":"DISABLED"},{"id":"ACCOUNTING","status":"DISABLED"}]}` — matches documented state exactly. Live SMS/WhatsApp/Weather/Accounting delivery is **not verifiable without owner access** (no vendor URL/token to configure; `providerConfig()` requires an HTTPS URL + token operators haven't set). `GET /api/v1/integrations/jobs` returns pre-existing seeded fixture rows (SMS/ACCOUNTING, various terminal statuses) from an earlier QA seed pass, confirming the endpoint and its shape work; no new jobs are created while providers are disabled (see I-004). | VERIFIED — MATCHES DOCS | — |
| I-003 | — | api | integrations (Weather) | `GET /api/v1/integrations/weather?project_id=<QA-SEED-ACTIVE>&latitude=17.385&longitude=78.4867` as `qa-admin-superadmin` | Per `docs/PROVIDERS.md`: "Unconfigured or unavailable weather is explicitly reported; no forecast is invented." | `{"status":"NOT_CONFIGURED","observed_at":null,"summary":null,"alerts":[]}` — no data fabricated, explicit unconfigured state, no 5xx. | VERIFIED | — |
| I-004 | — | api | integrations (Accounting export) | `POST /api/v1/integrations/accounting-export` with a QA-owned invoice id, as `qa-admin-superadmin` (global `inventory.manage`) | Per `docs/PROVIDERS.md`, accounting is disabled by default; export should fail closed with no job silently created | `503 {"code":"PROVIDER_NOT_CONFIGURED","message":"Accounting provider is not configured","retryable":true}` — the route checks `providerConfig('ACCOUNTING').enabled` *before* touching `provider_jobs`, so no half-created export row is left behind. Fail-closed confirmed. Full accounting round-trip (payload encrypted → vendor POST → receipt polling → `DELIVERED`) is **not verifiable without owner access** (no accounting vendor selected, per PROVIDERS.md's explicit "operators choose their own vendor" stance). | VERIFIED — FAIL-CLOSED | — |
| I-005 | — | api | reports (encrypted export round-trip) | `POST /api/v1/reports {type:"projects",format:"csv"}` then `GET /api/v1/reports/:id/download`, as `qa-admin-superadmin` (this is the "exports" item in the brief: `content_encrypted = encryptPii(base64(content))` at generation, `readBlob()`/`decryptPii()` at download — the same encrypt-at-rest path used for task evidence and legacy-upload fallback) | Generated report content round-trips through encryption unchanged | `POST` → 201 `READY`, 72 rows. `GET .../download` → 200, `content-type: text/csv`, 7655 bytes, correct header rows (`# Report: projects`, `# Data scope: Organization`), containing the seeded QA project data. Encrypted-at-rest → decrypt-on-download round trip verified working end to end on live data. | VERIFIED | — |
| I-006 | — | api | jobs (reverse geocoding) | Punched `CHECK_IN` for `qa-mob-employee` with real coordinates (17.385044, 78.486671 — Hyderabad), then polled `GET /attendance/me` across worker passes | `apps/api/src/modules/jobs/placeNames.ts`: punch starts "resolving", a worker pass names it via the public reverse geocoder within a bounded batch | Punch created with `place_status:"resolving"`, `place_name:null`. Within one worker pass (~3–5 min) `check_in_place_name` became `"Ward 78 Gunfoundry, Hyderabad, Telangana"`, `check_in_place_status:"named"`. `GEOCODING_REVERSE` is on (default) in this deployment; the naming job works correctly against a real provider. | VERIFIED | — |
| I-007 | — | api | jobs (scheduler / worker pass) | `systemctl list-timers silverline-worker.timer` + `journalctl -u silverline-worker.service` over ~10 consecutive passes | `silverline-worker.timer` fires roughly every 5–5.5 min per `docs/OPERATIONS.md`; each pass calls `/api/v1/jobs/run` and returns a JSON summary with no errors | 10/10 consecutive passes completed in 200–225s each with no service failures, e.g. `{"events":1,"deliveries":2,"survey_alerts":0,"survey_alert_mail":0,"duration_ms":224357}`. `events`/`deliveries` counters moved in response to QA actions taken during this task (task creation, evidence upload) confirming `runScheduledJobs`, webhook delivery, push delivery, provider jobs and place-naming all execute every pass under the single advisory-lock worker (`runJobs` in `apps/api/src/modules/automation/worker.ts`) without one stage's failure stopping the rest. `survey_alerts`/`survey_alert_mail` stayed 0 for every pass observed — no QA survey data currently trips any of the four alert conditions, and `SURVEY_MAIL_WEBHOOK_URL` is unconfigured (mail stays `QUEUED`, reported as waiting rather than failed, per `drainSurveyAlertMail`'s own contract) — consistent with an unconfigured relay, not a defect. | VERIFIED | — |
| I-008 | DECISION | api | integrations (notifications: SMS/WhatsApp/push/email) | Checked whether SMS/WhatsApp/push/survey-mail have a working "dev sink" to exercise end-to-end without a real vendor | None of SMS, WhatsApp, Expo push, or survey-alert mail have a vendor/relay/Expo project configured on this VM (`PROVIDER_SMS_ENABLED`/`PROVIDER_WHATSAPP_ENABLED` unset per I-002, `PUSH_ENABLED` has no status endpoint and no device with a real Expo token is registered, `SURVEY_MAIL_WEBHOOK_URL` unset per I-007). Code review of `apps/api/src/modules/integrations/worker.ts`, `apps/api/src/modules/jobs/push.ts` and `apps/api/src/modules/jobs/surveyMail.ts` confirms each no-ops safely (skips the provider loop, or leaves rows `QUEUED`/reports "waiting") rather than failing or fabricating delivery when unconfigured. **Not verifiable end-to-end without owner access** (a real SMS/WhatsApp vendor, an Expo project + real device token, or a mail relay URL) — matches the brief's guidance to record this rather than block on it. | NOT VERIFIABLE WITHOUT OWNER ACCESS | — |
| I-009 | — | web+mobile | RA bills, expense claims, purchase orders, leave requests, survey villages | Compared the exact API call each surface makes for 5 record types (read `apps/web/app/*/page.tsx` + `apps/web/lib/*.ts` against `apps/mobile/src/api/endpoints.ts`), then live-fetched each of: RA bill `da758f0c…`, expense claim `03df61dd…`, PO `de61beb2…`, a freshly-created QA leave request, and survey village `b2657aeb…` | Same totals/status/labels on both surfaces | All 4 of RA bill/expense/PO/leave hit the **identical** endpoint from both apps (`GET /api/v1/ra-bills/:id`, `/expense-claims/:id`, `/purchase-orders/:id`, `/leave/requests/:id`) with no separate transform layer, so identical JSON is structural, not incidental — confirmed live (e.g. RA bill: `status:"CERTIFIED"`, `net_payable:"135165.00"` on the one response both clients consume). Status is rendered as the raw API enum value on both sides (verified in `apps/web/app/expenses/page.tsx` and `apps/mobile/src/expensesFormat.ts` — neither remaps to a separately-maintained label string, so there is no label to drift). Survey villages are the one exception by design: `VILLAGE_STATUS_LABELS` (`packages/shared/src/survey.ts`) is imported only by `apps/web/app/survey/page.tsx`/`apps/web/lib/survey.ts` — mobile has no PM-style village-status browser at all; its only survey screen (`apps/mobile/app/(tabs)/survey.tsx`) is a crew-facing daily-return form driven by `getMyVillages()` (`/api/v1/survey/me/villages`), a different, narrower endpoint with different fields (`filed_today`, no `status_override`). This is an intentional scope split (crew entry vs. PM oversight), not a data inconsistency — there is no screen on mobile that could show a village status that disagrees with web. | VERIFIED — NO DRIFT (survey village screens are scoped differently by design) | — |

## Summary

Every integration in `docs/PROVIDERS.md` was checked live against the VM. All results match the
documented "no vendor selected, disabled by default, fail closed / explicit-unconfigured" design.
The malware scanner, encrypted-export round trip, reverse geocoding and the background worker
pass are all fully working and were exercised end to end with real QA data. No code defects were
found; nothing needed fixing under Task 3's rules. The one non-trivial discovery (I-001) is a
methodology note about ClamAV's own EICAR-anchoring behavior, kept here so it isn't rediscovered
the hard way next sweep.
