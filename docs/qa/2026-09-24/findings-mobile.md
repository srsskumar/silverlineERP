# Findings ledger — Mobile deep QA (T5.5)

Companion to `docs/qa/2026-09-24/coverage.md` (already has a per-module static
walk of every mobile screen) and `findings-a.md`/`findings-b.md` (lane A/B,
including mobile parity notes: A-001 inbox no-navigate, A-002 my-payslip
comment, B-006 requisition creation confirmed present on mobile, B-008 stock
module has no mobile UI, B-009 documents read-only, B-010 planning read-only,
B-011 pipeline/clients/tenders read-only except lead-stage). This ledger adds
what those static passes did not do: live API calls against dev-thor with
real QA- data and every seeded role, and write-path attacks.

Scope note: `apps/mobile/app/payables.tsx` and `expenses.tsx` are owned by
the parallel `qa-gaps` lane — reviewed and logged here (M-003), not fixed.

| ID | Sev(P0-P3/DECISION/GAP) | Surface | Module | Steps | Expected | Actual | Status | Fix commit |
|---|---|---|---|---|---|---|---|---|
| M-001 | P1 | mobile | approvals, procurement, inventory, pipeline, reports | Live-called `POST /tasks {project_id:"not-a-uuid", title:""}` as `qa-mob-employee` giving `422 {"code":"VALIDATION_ERROR","message":"Validation failed","field_errors":[{"field":"project_id",...},{"field":"title",...}]}`. Read every mobile write screen's catch block (`grep -rn "instanceof Error ? e.message" `) | A 422 with several bad fields tells the user which field and why | `client.ts` already parses `field_errors` onto `ApiError.fieldErrors`, but `approvals.tsx` (decide/recall), `procurement.tsx` (raise/submit requisition), `inventory.tsx` (post transaction), `pipeline.tsx` (lead stage) and `reports.tsx` (generate/save) all rendered only the top-level `message` -- a fixed, generic "Validation failed" whenever more than one field failed zod validation. Screens whose writes go through the offline queue (leave, tasks, attendance, assets) already had this right via `queueCore.ts`'s `describeRequestError`; the gap was specifically the five screens that call `postXxx()` directly. | FIXED | 858f36e |
| M-002 | P2 | mobile | attendance (tab) | Read `src/rbac.ts`'s `TAB_PERMISSIONS` doc comment ("screens that lack permission show a locked-state message instead of data"), then grep for consumers -- zero. Live-checked `qa-admin-client` (CLIENT_VIEWER, permissions `project.read, task.read, cycle.read, notification.read, dashboard.read, board.read`, no `attendance.*`) against `GET /attendance/me` | The Attendance tab shows a locked-state message, matching Assets (`canDo("asset.read")` gate) and Survey (`canDo("survey.enter")` gate) | Got `404 NO_EMPLOYEE_LINK` from the API, silently swallowed -- the screen rendered the full punch UI (map, Check in/Check out) unconditionally, with no permission check anywhere in the file, and fired a device GPS permission prompt on mount for a user who could never punch | FIXED | a1cfe26 |
| M-003 | P1 | mobile | expenses (owned by qa-gaps lane) | Same grep as M-001 against `app/expenses.tsx` | N/A -- logged only, not fixed (owned by the parallel lane per this task's brief) | Same bug shape as M-001: `postExpenseClaim`/`postExpenseClaimSubmit`/`postExpenseClaimWithdraw`'s three catch blocks (`app/expenses.tsx:152,166,180`) all do `e instanceof ApiError ? e.message : "<fallback>"`, dropping `field_errors` on a multi-field 422 | OPEN -- logged for the qa-gaps lane | -- |
| M-004 | P2 | mobile | clients, tenders, pipeline (leads), procurement (requisitions, purchase orders) | Live-called each list endpoint (`GET /clients?limit=50`, `/tenders?limit=50`, `/leads?limit=50`, `/requisitions?limit=50`, `/purchase-orders?limit=50`) -- all return `has_more`/`next_offset`. Grepped all four screens for "hasMore" -- zero matches | Some way to see past the first page -- a "Load more" control or at least an indicator, matching `asset-movements.tsx`'s own offset-tracking pattern | `getClients`/`getTenders`/`getLeads`/`getRequisitions`/`getPurchaseOrders` (`src/api/endpoints.ts`) all fetch and return `hasMore`, but none of the five screens ever reads it -- a page beyond the first 50 rows was silently invisible with no indication more existed. | FIXED (Task 5d) | 52071d0 |
| M-005 | P3 | mobile | leave (tab) | Same check as M-002 applied to `app/(tabs)/leave.tsx` | Locked state when neither `leave.request` nor `leave.read` is held | No gate either -- same shape as M-002. | FIXED (Task 5d) | f3518c6 |
| M-006 | P3 | mobile | more (notification preferences) | Read `app/(tabs)/more.tsx`'s `setPreference()` -- its catch block is bare `catch { setPreferenceError("Connect to the internet to update notification preferences.") }`, no inspection of the thrown error at all. Live-called `PATCH /auth/preferences {not_a_real_channel:true}` giving `422 VALIDATION_ERROR` (not a connectivity failure) | An error message that reflects what actually happened | The UI can only ever say "Connect to the internet...", even for a genuine 403/422/500 while fully online -- low real-world risk since the three toggles (`push`/`sms`/`whatsapp`) always send a valid `{key: boolean}` the schema accepts, but misleading if a future channel or a server-side policy ever rejects a real user's toggle | OPEN | -- |
| M-007 | -- | api | leave | Live idempotency-key attack: `POST /leave/requests` with key A gives 201; replay same key A with the same body gives 200 (same resource, no duplicate); a genuinely new request with the same body but a fresh key gives 422 LEAVE_OVERLAP (server's own date-overlap rule, not a duplicate) | Idempotency-Key dedups a retried submit; a real second attempt is governed by business rules, not silently duplicated | Confirmed exactly that -- clean | VERIFIED -- no bug | -- |
| M-008 | -- | api | auth (preferences) | Live-attacked `PATCH /auth/preferences` with an unrecognised key (`{not_a_real_channel:true}`) and a wrong-typed value (`{push:"yes"}`) | 422 with a specific field-level reason | Got `field_errors` naming the unrecognised key and, separately, "Push must be yes or no" for the wrong-typed value -- clean, specific, no 500 | VERIFIED -- no bug | -- |
| M-009 | -- | api/mobile | projects, planning, analytics, billing, automation | Live-called `GET /projects/:id`, `/cycles?project_id=`, `/analytics/projects/:id`, `/insights/projects/:id`, `/automation-rules?project_id=`, `/projects/:id/ra-bills`, `/ra-bills/:id` first as `qa-admin-pm` (a PROJECT_MANAGER scoped to one specific project) against an out-of-scope project (QA-SEED-ACTIVE) -- all seven 403'd. Re-ran the same seven against `qa-admin-admin` (org-wide) and against the PM's own scoped project | 403 on an out-of-scope project for a scoped role; 200 with a shape matching the TS interface for an in-scope/unscoped caller | Confirmed exactly that on all seven -- project-scope enforcement is consistent across every mobile-reachable project-scoped endpoint, and every response shape (ProjectDetail.counts, Cycle.metrics, ProjectAnalytics.summary/flow/cycles/workload, ProjectInsight.prediction/factors, RaBill.gross_value/net_payable/items) matched `src/api/endpoints.ts`'s interfaces exactly, including the `[k:string]:unknown` catch-all fields the mobile client doesn't type but tolerates | VERIFIED -- no bug | -- |
| M-010 | -- | mobile | (broad GET sweep) | Live-called ~35 GET endpoints across `qa-mob-employee`, `qa-admin-hr`, `qa-admin-pm`, `qa-admin-admin`, `qa-admin-inventory`, `qa-admin-payroll` covering attendance/tasks/leave/notifications/preferences/employees/projects/documents/approvals/inventory/clients/leads/tenders/ra-bills/ageing/requisitions/purchase-orders/payroll runs/cycles/reports/asset-movements. Compared every response's top-level and one-level-nested keys against the TS interfaces in `src/api/endpoints.ts` | Field names and envelope shapes (`{data,has_more,next_cursor}` vs bare array vs `{data:{...}}`) match what the client's `asList`/`asItem`/`asPage` helpers and each screen expect | No shape mismatches found anywhere in the sweep -- `asList`/`asItem`/`asPage`'s tolerant-envelope design (accepts enveloped or bare) absorbs most of the risk class that caused 2 P0s on web, and every interface's optional/`[k:string]:unknown` fields matched what the server actually sends | VERIFIED -- no bug across the sweep | -- |
| A-001 | P1 | mobile | inbox (screen), tasks (tab, deep link) | Task 5d. Read `app/inbox.tsx`'s `openRow()` -- marked read, never navigated. Mirrored web's `inboxEntityHref` (`apps/web/lib/notifications.ts:157`), which itself now reads a server-resolved `href` (`apps/api/src/modules/s5/routes.ts`'s `NOTIF_COLS`) | Tapping a notification marks it read AND opens the right mobile screen, same as web's InboxList (§note 14) | New `src/deepLinks.ts` (pure, 12 tests) maps the server's web `href` to a mobile expo-router path per entity type (task, leave, report, expense, procurement, ra_bill→project-finance, asset, employee, survey_village, and a LEAVE\* local fallback for hrefless responses), validating every id as a UUID first and returning `null` (mark read, "open on web" note) for anything malformed or web-only (`/admin`). Also closed the one pre-existing deep link's own gap: `app/(tabs)/tasks.tsx`'s `taskId` param is now UUID-validated before it reaches `getTask()`, with a not-found banner for a malformed id and a not-found EmptyState in the sheet for a valid-but-inaccessible one. | FIXED | 9ea6a8e |
| B-009 | P2 | mobile | documents | Task 5d. `apps/api/src/modules/documents/routes.ts` has had `POST /documents/:id/renew` since S46 (`document.manage`); `documents.tsx` was fully read-only | A renew action gated like web's RenewDialog | Added a "Renew" button (hidden once `state==='SUPERSEDED'`, avoiding the server's 409 `ALREADY_RENEWED`) and a form for the new expiry date (required, mirroring web), an optional issue date and reference number, matching `documentRenewSchema` exactly. Uploading a new file stays out of scope -- no Expo file-picker module is already in package.json, and documents is an index over other modules' own file stores, not a file store of its own. | FIXED | c9c6b8c |
| B-011 | P2 | mobile | pipeline (leads), clients, tenders | Task 5d. `pipeline.tsx` was lead-stage-only; checked whether clients/tenders creation were cheap to add per the brief (~30 min each) | A lead create form gated by `lead.manage` with fields matching `leadSchema` exactly, and client/tender creation added if cheap | Added "New lead" (`lead_no`, `organization_name`, `lead_type`, `source` -- `leadSchema`'s required fields, no stripped/invented fields). Checked `clientBaseSchema`/`tenderBaseSchema`: each requires only 2 fields (name+client_type; tender_no+tender_type, with every conditionally-required field defaulting safely when omitted) -- cheap, so added "New client" and "New tender" too, each under its own `client.manage`/`tender.manage` gate. | FIXED | 52071d0 |
| M-011 | P0 | api/mobile | tasks (quick-add) | Task 5d adversarial sweep. Live-created a task via `POST /tasks {project_id, title}` (no `assignee_id`, matching `tasks.tsx`'s quick-add payload exactly) as `qa-mob-employee`, then attacked `POST /tasks/:id/evidence` on that same task with the same token | A task's own creator can act on it (transition, comment, attach evidence) | 403 `FORBIDDEN`: "That task belongs to somebody else... anything else needs the \"task.assign\" permission." Confirmed by reading `apps/api/src/modules/work/routes.ts`'s own PRD §4 comment: an EMPLOYEE holding `task.update` without `task.assign` may only act on tasks assigned to themselves -- and quick-add never set `assignee_id`, so every task a field employee "Add"ed on the Tasks tab became one they personally could not touch again. Fixed on mobile (no API change): `src/tasksFormat.ts`'s `quickAddAssigneeId()` self-assigns the new task only when the caller cannot assign elsewhere anyway (`!canDo("task.assign")`); a caller who holds `task.assign` keeps the old unassigned-by-default behaviour, since they may be quick-adding for someone else. Live-reproduced before (403) and confirmed fixed after (201, same task, same caller). | FIXED | 73edfac |
| M-012 | -- | api | attendance/events | Task 5d adversarial sweep. Live-attacked `POST /attendance/events` as `qa-mob-employee`: out-of-range/huge/wrong-typed latitude and longitude, latitude without longitude, a client_timestamp 30 min in the future and 45 min in the past, `mock_location:true`, and a same-(employee,event_type) replay inside the 5-minute dedup window with a fresh Idempotency-Key each time | Bad coordinates 422 with a field reason; large clock skew (either direction) queues for review rather than a hard 500/crash; a replay inside the dedup window is a no-op, not a duplicate row | All clean: coordinate/type/together-constraint violations 422 with a precise field error; skew beyond 15 min (either direction, including the 30-min-future case) returns 202 `TIMESTAMP_SKEW` for review rather than an outright refusal (`FUTURE_PUNCH` is reserved for a narrower 5-15 min-ahead band); the dedup window returned a plain 200 no-op on replay, confirmed twice (once by an intentional A8 replay, once incidentally when A7's mock-location attempt landed inside A6's own dedup window -- which is itself further live confirmation the window works, though it means `mock_location:true`'s own 202/`MOCK_LOCATION` review path was not independently isolated this round: every attempt against this one seeded mobile account and work-day either collided with a prior event or was still in-window). No fix needed. | VERIFIED -- no bug (MOCK_LOCATION review path not independently isolated, see note) | -- |
| M-013 | -- | api | tasks/:id/evidence | Task 5d adversarial sweep. Live-attacked `POST /tasks/:id/evidence` (self-assigned own-org task, per M-011's fix): disallowed extension (`evil.exe`), a double-extension trick (`photo.jpg.exe` -- last extension governs), no extension at all, a >5MB decoded payload, and malformed base64; separately, a task created live in a second org (`qa-admin-org2`) attacked with the org1 employee's token, and a well-formed but never-seeded task id | Extension/size/encoding attacks 422 with a field reason, never a 500; a cross-org or nonexistent task id 404s without confirming whether it exists elsewhere | All clean: every extension/size/base64 attack came back 422 with a specific field_error (`"Only pdf, jpg, jpeg, png files are allowed"`, `"File exceeds the 5MB limit"`, `"Invalid base64 content"`), a valid PNG upload alongside them still succeeded (201), and both the cross-org and the nonexistent task id returned the same `404 NOT_FOUND "Task not found"` -- no information leak about which case it was. No fix needed. | VERIFIED -- no bug | -- |
| M-014 | -- | api | survey/entries | Task 5d adversarial sweep. Live-attacked `POST /survey/entries` as `qa-mob-employee`: a future `entry_date`, a negative `crew_present`, an unknown measure code, an absurdly large quantity value, an invalid `low_progress_reason`, and a nonexistent `survey_village_id` | Each schema violation 422s with the field and a human reason; an inaccessible village 404s without a crash | All schema-level attacks (future date, negative headcount, oversized quantity, bad enum) 422'd with the exact human message `packages/shared/src/survey.ts` defines. The unknown-measure-code and nonexistent-village attacks both 404'd rather than reaching the code-specific `UNKNOWN_MEASURE` check -- this test account (`qa-mob-employee`) is not crewed to any survey village (confirmed separately: `GET /survey/my-villages` also 404s for it), so `villageOr404`'s access check rejects every `survey_village_id` it tries, real or not, before the handler's own measure-code check runs. Schema validation is confirmed airtight; the deeper UNKNOWN_MEASURE business check was not independently live-exercised this round for lack of a crewed test account. No fix needed. | VERIFIED -- no bug (see note on scope) | -- |

## Attacked and found clean (no finding filed)

- Double-submit / idempotency across write screens: `postRequisition` (no
  explicit key passed, so `client.ts` auto-generates one) is guarded
  client-side by `disabled={submitting}` on its button, same pattern as every
  other submit button in the app; a genuine same-body resubmit after the
  button re-enables goes through `client.ts`'s auto-key path and is a real
  new request, correctly governed by server-side business rules (see M-007
  for the leave case, same mechanism).
- 401 handling (code review, not independently re-run live this round):
  `src/api/client.ts`'s single-refresh-then-logout path (lines 286-312) is
  already covered by `retry-after.test.ts`/`replay-body.test.ts`/
  `remote-wipe.test.ts`; reads correctly (a revoked-device 401 skips refresh
  entirely and wipes; an ordinary expired-token 401 gets exactly one refresh
  attempt, then signs out with a typed ApiError).
- Reversed-date leave request server-side: bypassing the client's own
  `validateLeaveRequest` and posting `from_date > to_date` directly still
  fails -- separately confirmed clean in `findings-a.md`'s round 1 (near
  A-004); not re-litigated here.

## Task 5d (2026-09-24): review notes from the prior pass, closed

- **errorFormat.ts / queueCore.ts duplication** (review note 5a): reconciled
  into one `describeApiError` helper, used by both the five direct-call
  screens and the offline queue's sync screen; the dead `instanceof Error`
  branch (unreachable -- a plain Error already satisfies the structural
  `isApiErrorLike` check) and the field-name/error-code prefixing (`"title:
  Title is required"`, `"DATE_RANGE: ..."`) are both gone -- output is now
  just the server's own already-humanised message(s). Commit 3456509.
- **Attendance shows punch controls to a read-only role** (review note 5b):
  `canPunch` now gates the map/location, the Punch card and Regularisation
  separately from `canAccess` (punch OR read); a read-only viewer keeps
  Recent punches and gets a plain-text note instead of the punch UI. Commit
  f3518c6.
- **M-005** (Leave tab ungated): fixed the same round, same commit.

## Not covered this round (honestly listed)

Task 5d closed A-001, M-004, M-005, B-009, B-011, and ran the adversarial
sweep item 6 asked for (attendance punch payloads, task evidence upload,
survey entries -- M-011 through M-014 above). Still not independently
live-attacked, beyond what `coverage.md`'s static pass and
`findings-a.md`/`findings-b.md` already recorded:

- Survey GCP control-point creation's own payload sweep -- already covered
  live in an earlier round (`~/sl-e2e/qa-boundary.mjs`), not re-attacked
  either round.
- The `MOCK_LOCATION` review path in isolation (see M-012's note -- every
  attempt against the one seeded mobile test account collided with a prior
  punch's dedup window or the account's own daily state).
- The survey `UNKNOWN_MEASURE` business check in isolation (see M-014's
  note -- the seeded mobile test account is not crewed to any village, so
  every `survey_village_id` 404s before that check runs).
- Documents, org-holidays, employees, project-finance, receivables,
  automation, analytics, asset-movements screens -- read-only per
  `coverage.md`'s static walk (confirmed live only incidentally, via the
  M-010 GET sweep, not independently attacked for pagination/error-state
  edge cases beyond what's logged above; documents gained its one write
  action, B-009, this round).
- Offline queue replay/duplicate semantics beyond what `queue.test.ts`/
  `outbox-housekeeping.test.ts`/`replay-body.test.ts` already assert (no
  live airplane-mode device walk was possible -- no emulator/device
  available, per this task's constraints).
