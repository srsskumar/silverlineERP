# Findings ledger — Lane B (dashboard, projects, planning, my-work, automation, analytics, reports, leads/clients/tenders, billing/receivables/payables/expenses/ledgers, procurement, inventory/stock, assets/asset-movements, documents, approvals, survey, record)

Companion to `docs/qa/2026-09-24/coverage.md`. Lane A's ledger is `findings-a.md` (kept separate so
the two lanes never conflict on the same file; merge into one ledger at the end of the sweep).

| ID | Sev(P0–P3/DECISION/KNOWN-TLS) | Surface(web/mobile/api) | Module | Steps | Expected | Actual | Status | Fix commit |
|---|---|---|---|---|---|---|---|---|
| B-001 | P2 | api | automation | GET /automation-rules/:id | readable with automation.read | requires automation.manage | FIXED (see B-019) | cfca915 |
| B-002 | P2 | api/web | billing/payables | Approve a payment run via `POST /api/v1/payment-runs/:id/decision` (`paymentrun.approve`), then look for a way to mark it disbursed/paid — checked `apps/web/app/payables/page.tsx` (line 497 talks about "releasing" but no button follows it) and `apps/api/src/modules/ledgers/routes.ts` (only `paymentrun.manage` build + `paymentrun.approve` decide routes exist) | An execute/disburse action moves an approved run to "paid" | No such route or UI exists on any surface; approved runs have no further lifecycle step | OPEN — known gap, `open-decisions-2026-09-22.md` | |
| B-003 | P2 | api/web | expenses | Submit an expense claim and look for a receipt/file attachment control — checked `apps/web/app/expenses/page.tsx` (only prose at line 814 mentions receipts) and `apps/api/src/modules/expenses/routes.ts` (no multipart/attachment route on `expense-claims`) | A file input lets the claimant attach a receipt image/PDF before or after submit | No file input anywhere in the claim form; no attachment endpoint server-side | OPEN — known gap, `open-decisions-2026-09-22.md` | |
| B-004 | P2 | api/web/mobile | procurement | Search the repo case-sensitively for `vendor.invoice`, `vendor_invoice`, `VendorInvoice`, `msme` across `apps/api/src/modules` and `apps/web/app` | Some vendor-invoice-lines + MSME data model backs the three-way PO/GRN/invoice match | Zero hits anywhere — the feature has no data model at all, not just a missing screen; three-way match is unreachable | OPEN — known gap, `open-decisions-2026-09-22.md` | |
| B-005 | P3 | web | procurement | Look for a requisition/PO/RFQ/GRN creation control in `apps/web/app/procurement/page.tsx` (755 lines) | A "New requisition"/"New PO"/"New RFQ"/"New GRN" form, matching the API's `requisition.manage`/`po.manage`/`rfq.manage`/`grn.manage` write routes | Zero creation controls found (`grep -n "MutationForm\|New requisition\|New order\|New RFQ\|Create"` → no matches); web is status-move/approve only | OPEN — narrower than assumed: mobile already covers requisition creation, see B-006 | |
| B-006 | INFO | mobile | procurement | Compare `apps/mobile/app/procurement.tsx:172-255` ("Raise" button → `submitNewRequisition`, gated `requisition.manage`) against the open-decisions assumption that requisition creation is missing on both surfaces | open-decisions-2026-09-22.md to be corrected — requisition creation exists on mobile | Confirmed present and correctly gated; PO/RFQ/GRN creation still missing on both web and mobile | OPEN — documentation correction, not a code gap | |
| B-007 | P2 | web/mobile | billing | Look for "New advance" / "New RA-bill" creation controls in `apps/web/app/billing/page.tsx` (1043 lines) and the mobile `project-finance.tsx` equivalent | A form to raise a fresh RA-bill or advance, matching `POST /api/v1/advances` and `POST /api/v1/ra-bills` (`apps/api/src/modules/billing/routes.ts:134,188`) | No such control on web (zero `MutationForm`/"New advance"/"New RA bill" hits); mobile not yet confirmed by this pass | OPEN — known gap, `open-decisions-2026-09-22.md` | |
| B-008 | P2 | web/mobile | inventory/stock | Look for any UI reachable from web or mobile for `stock-locations`, `stock-counts`, `stock-reservations`, `stock/reorder` (`apps/api/src/modules/stock/routes.ts`) | Multi-location stock, physical counts and reservations are manageable from at least one client | Zero references anywhere under `apps/web/app`, `apps/web/components`, or `apps/mobile/app` — the entire `stock` module (as opposed to the older single-location `inventory` module, which does have web CRUD) is API-only | OPEN — known gap, `open-decisions-2026-09-22.md`, broader than "creation screens" | |
| B-009 | P3 | mobile | documents | Look for a create/renew/legal-hold/delete action in `apps/mobile/app/documents.tsx` (225 lines) | At least renewal, matching web's `document.manage`-gated "New document"/"Renew" buttons | No create/renew action found — mobile is tab-switch + row-select only (read-only) | OPEN — newly found, not in open-decisions | |
| B-010 | P2 | mobile | planning | Look for cycle create/start/close, SLA config, workflow editor, custom-field, or bulk-task-move actions in `apps/mobile/app/planning.tsx` (175 lines) | At least one of the five web-side write actions (`cycle.manage`, `project.update`, `board.manage`, `custom_field.manage`, `task.update`) present on mobile | None found — mobile Planning renders `CycleGroups`/`CycleRow` only, no reference to any of the five permissions in the file | OPEN — newly found, not in open-decisions | |
| B-011 | P3 | mobile | leads/clients/tenders | Look for "New lead"/"Add a client"/"New tender" creation forms in `apps/mobile/app/pipeline.tsx`, `clients.tsx`, `tenders.tsx` | At least stage-transition parity (confirmed present in pipeline.tsx) plus some create path | Zero create forms on any of the three screens; clients.tsx and tenders.tsx are fully read-only (no write-permission reference at all in either file) | OPEN — newly found, not in open-decisions | |
| B-012 | P2 | api | reports | Call `POST /api/v1/reports` or `GET /api/v1/reports/:id/download` as a user with `report.generate` but missing the underlying per-type data permission (e.g. a payroll report without `payroll.read`) | 403 with the documented message ("needs both the report.generate permission and permission to read the data it covers") — `apps/api/src/modules/s6/routes.ts:758,882`; both routes' `preHandler` is bare `authenticate`, the real check is inline | Not yet exercised — flagged from static reading; a lane walker must call both routes on web and mobile to confirm parity | OPEN | |
| B-013 | P1 | api | procurement/approvals | Seeded (task-2 report, finding 1). Create a PO under a policy needing one approval level, submit it, have the approver `POST /approvals/:id/decision {decision:"APPROVE"}` | `GET /purchase-orders/:id` reflects `status:"APPROVED"` once the ladder clears — matches the web page's own "Move to" copy ("an order reaches approved only when its ladder says so, never by moving it here") and requisitions' existing behaviour | `reflectOnDocument()` in `approvals/routes.ts` only handled `PURCHASE_REQUISITION`; a fully-approved PO stayed at `PENDING_APPROVAL` forever, and the test suite (and task-2's seed) had to write the row directly to work around it | FIXED | 3972ed4 |
| B-014 | P2 | api | procurement | Seeded (task-2 report, finding 2). `POST /invoices` with a `purchase_order_id`, then `POST /invoices/:id/match` | The invoice persists its PO link and `/match` runs the real three-way-match logic against it | `invoiceSchema` (`packages/shared/src/v2.ts`) had no `purchase_order_id` field, so it could never be set via the API; `/match` always 422ed `NO_PURCHASE_ORDER`. DB column + index already existed (migration 038) — only the write path was missing | FIXED (link only) — three-way match still can't *succeed* for an API-created invoice because no route writes `invoice_lines`; that broader gap stays tracked as B-004 for Task 5 | b03d0cd |
| B-015 | P2 | api | approvals | Seeded (task-2 report, finding 3). `POST /approvals/:id/decision` with no `if-match`, or with the underlying document's version instead of the approval instance's | A message that says whose version is wanted | Shared `version()` helper's message ("If-Match must contain the current version" / "This record changed. Reload before editing.") never named the approval instance — a caller naturally reaches for the document version already in hand, as task-2's seed script initially did | FIXED — `version()` takes an optional label, used only by the decision route so every other caller's message is unchanged; chose the clearer-message reading over forgiving either version (accepting either risks silently deciding against a stale instance version) | f934f84 |
| B-016 | P2 | api | documents (framework-wide) | Live probe: `DELETE /documents/:id` with a valid token, `if-match`, and `content-type: application/json` but no body (reproduced with a fresh curl-equivalent script on dev-thor, then a vitest `fastify.inject` repro) | The delete completes (or fails with a real business code: RETENTION_BLOCKED/FORBIDDEN/etc.) | Fastify's default JSON body parser throws on an empty body with that header; the global error handler flattens it into a generic `400 BAD_REQUEST "Bad request"` before the route's own permission check or business rules run. Mobile's client (`apps/mobile/src/api/client.ts:237-240`) sets `Content-Type: application/json` on every request regardless of body — web avoids this by only setting it when a body is sent, but the server contract shouldn't rely on that. No mobile route calls a bodyless DELETE today, so nothing live was broken, but the next one added would silently hit this | FIXED — registered an `application/json` content-type parser that treats an empty body as no body; malformed JSON still fails loudly | c3d2cb8 |
| B-017 | INFO | api | billing/tenders | Live probe via pre-existing `~/sl-e2e/fin/billing.mjs` and `crm.mjs` found live dummy data with a certified RA-bill at `certified_amount: -5000.00` and a tender at `estimated_value: -5.00` | N/A | Both are stale artifacts from Sep-22 probing against an *older* deployed build — `git log` on this branch shows both gaps already fixed upstream (`b221143 fix(billing): certify a bill for a real amount...` adds `.nonnegative()` to `certified_amount`; `0f11bb9 fix(tenders): validate what a corrigendum changes`). Re-running both negative-amount attempts against the current code gets a proper 422. Not a Lane B finding — noted so a successor doesn't re-chase it; the live dummy data itself is still contaminated (QA-FIN test records, not seeded QA- data from task 2) and could be cleaned up or ignored per the "live data is dummy" decision | N/A — verified not reproducible on current code | |
| B-018 | INFO | api | procurement/billing/CRM/inventory/documents/survey | Live cross-org IDOR sweep (`~/sl-e2e/lane-b/idor-b.mjs`): read 17 org-1 QA-seeded resources (project, RA-bill, payroll run, asset, inventory item, client, lead, tender, vendor, requisition, PO, invoice, GRN, expense claim, document, survey programme, survey village) using the org-2 admin token | 403/404 on every read | All 17 returned 404 `NOT_FOUND` — clean tenant isolation across every Lane B resource type checked | VERIFIED — no cross-org leak found | |
| B-019 | P2 | api | automation | Same defect as B-001, closed with root-cause fix + test: `GET /automation-rules/:id` preHandler changed from `automation.manage` to `automation.read`, matching the list and executions routes | An `automation.read`-only holder (e.g. AUDITOR) can open a rule's detail after seeing it in the list | Previously 403 FORBIDDEN | FIXED | cfca915 |
| B-020 | Important (review, fix round 1) | api | procurement/approvals (framework-wide) | Review finding on c3d2cb8/B-016: send `POST /purchase-orders/:id/status` and `POST /approvals` with `content-type: application/json` and no payload | 4xx, never a 500 | The B-016 parser mapped an empty body to `undefined`; ~15 handlers do `const body = req.body as {...}` with no null check (e.g. `body.status` at procurement/routes.ts:277, `body.document_type` at approvals/routes.ts:200-201) and threw a TypeError, surfaced as `500 INTERNAL_ERROR` — worse than the 400 B-016 fixed | FIXED — parser now maps an empty body to `{}`; zod `parse()` rejects a required field missing from `{}` exactly as it did from `undefined`, so no route's required-field validation changed | d282337 |
| B-021 | Important (review, fix round 1) | api | procurement (inventory/routes.ts) | Review finding on b03d0cd: `POST /invoices` with `vendor_id` = vendor B and `purchase_order_id` pointing at a PO that belongs to vendor A (same org) | 422 and no invoice row created | `purchase_order_id` was checked for org membership only, never against the invoice's own `vendor_id` — the invoice was created and linked to another vendor's PO (201) | FIXED — fetches the PO (already required for the org check) and compares `vendor_id`; rejects with a stable `PO_VENDOR_MISMATCH` code. No PATCH/update route exists for invoices today, so the create path was the only write path to guard | fdec0c2 |
| B-012 | P2 | api | reports | Round 2: called `POST /api/v1/reports {type,format:"csv"}` for every `REPORT_TYPES` value as `qa-fin-inv` (INVENTORY_MANAGER — has `report.generate` + `inventory.read`/asset perms but not `project.read`/`payroll.read`/etc), and `GET /reports/:id/download` on an id it doesn't own | 403 FORBIDDEN with the documented "needs both..." message on domains it lacks read for; 201/202 on domains it holds | Confirmed exactly that: 403 on employees/payroll/projects/cycles/audit/tasks/leave/attendance, 201 on assets/inventory/invoices; download of a nonexistent/foreign id returns 404 NOT_FOUND. Also generated + downloaded all 10 report types as admin (csv) — every one returns 200 with real headers/rows, none empty or malformed | VERIFIED — no bug, matches documented behaviour; closing the OPEN flag from round 1 | |
| B-023 | P0 | web/api | projects | Round 2 Playwright-motivated form audit (parallel lane found the same shape of bug on employee edit): read `apps/web/components/projects/ProjectForm.tsx`'s `toPatchPayload()` — "Project manager" and "Project type" render editable (no `disabled={editing}`, unlike workspace/code) and are sent whenever set, alongside `name` (always present). Live-verified: `PATCH /projects/:id {name, project_manager_id}` as admin | 200, version bumps, and a re-`GET` shows the new `project_manager_id` | 200, version bumped — but re-`GET` still showed `project_manager_id: null`. `projectPatchSchema` (`packages/shared/src/s4.ts`) had no key for `project_manager_id`/`project_type_id`, so zod's default strip-unknown-keys mode silently dropped both; because `name` is always sent too, the request never fails, so nothing — not even a toast — tells the user the field they just picked was ignored. Same class of bug as the parallel lane's employee-edit P0: a save that reports success and silently changes nothing | FIXED — added both fields to `projectPatchSchema` (mirroring the org-scoped existence checks `projectCreateSchema`'s route already runs for the same two columns) and wired the PATCH route's `UPDATE` to write them. RED: two new tests in `apps/api/test/s4.test.ts` ("editing a project's manager and type (B-023)") — moves-on-PATCH failed `expected null to be '<uuid>'`; cross-org manager failed `expected 200 to be 404`. GREEN: `apps/api/test/s4.test.ts` 57/57, `apps/web/tests/s4.test.ts` 37/37, `packages/shared` 1001/1001 | d0b4e2b |
| B-025 | INFO | web | leads/clients/tenders | Round 2 Playwright submit-walk (`scripts/qa/e2e/lane-b/submit-walk.mjs`) on the live VM: as the lowest allowed role for each form (`qa-fin-sales` for `/leads/new` and `/clients`, `qa-fin-tender` for `/tenders/new`), filled valid QA- data and submitted; then resubmitted a duplicate unique identifier (lead/client/tender number); then confirmed `qa-fin-emp` (holds none of `lead.manage`/`client.manage`/`tender.manage`) is turned back at all three | Valid submit → 201 + "Saved successfully"; duplicate → a clear non-silent error; disallowed role → blocked, not a bare page | All three valid submits: 201 + saved banner. All three duplicates: 409 with `role=alert` text "That did not work / A record with this identifier already exists" (console also shows the 409, as expected). `qa-fin-emp` correctly blocked at `/leads/new` and `/tenders/new` (client-side "forbidden" copy via `RequireDestination`). One artifact along the way: running the boundary check for `qa-fin-emp` across three pages in one browser context/session (three fresh `/auth/login` calls in quick succession across the whole run) once hit the account's own sign-in rate limiter (`429 RATE_LIMITED`), which the web app's `AuthProvider` surfaced as "Your session has ended" rather than a per-page 403 — confirmed by reproducing the same `RATE_LIMITED` code directly against `/auth/login`; not a bug a real user would hit (nobody re-authenticates once per page), but noted since it looked like a session bug at first glance. Script since isolates each boundary check into its own login/context and retries once on `RATE_LIMITED` | VERIFIED — no code defect on any of the three forms; the rate-limit-looks-like-signout wrinkle is a test-script artifact, tagged here so a successor doesn't re-chase it | |
| B-024 | P1 | web/api | inventory | Following B-023's pattern, checked the other generic table-driven routes in `apps/api/src/modules/inventory/routes.ts` (`for (const [path,table,schema,permission] of [vendors, inventory/items, assets])`) — `PATCH /:path/:id` parses the request with the exact same zod schema `POST` uses to create the row. `itemSchema`/`vendorSchema` default `status` to `'ACTIVE'`; the web inventory-item edit form (`apps/web/app/inventory/page.tsx`'s `itemFields`) never includes a `status` field at all. Live-verified: deactivate an item (`PATCH {..., status:'INACTIVE'}` → 200, confirmed `INACTIVE`), then PATCH again with exactly the field set the edit form sends (no `status`) | Editing an item's name/threshold/cost leaves its `status` alone | The second PATCH returned `200` with `status:'ACTIVE'` — the edit silently reactivated the item. (Asset edit form does include `condition`, its equivalent defaulted field, so assets were unaffected; vendors have no web edit UI at all, so only reachable there via direct API use) | FIXED — after a successful `parse()`, the PATCH handler now keeps only the keys actually present in the raw request body before building the `UPDATE`; every currently-required field is unaffected since parsing itself would already fail without it. RED: new test in `apps/api/test/v2.test.ts` ("does not silently reactivate an item on an unrelated PATCH — B-024") — deactivate, then edit-shaped PATCH, assert `status` stays `INACTIVE`; failed `expected 'ACTIVE' to be 'INACTIVE'` pre-fix. GREEN: `apps/api/test/v2.test.ts` 47/47, `test/catalogue/procurement.test.ts` 55/55, `test/s4.test.ts` 57/57, `test/catalogue/e2e.test.ts` 46/46 | 8ff18d1 |
| B-022 | P1 | web/api | dashboard | Round 2 unwalked-module pass: read every `widget()` call across `buildAdminWidgets`/`buildHrWidgets`/`buildPmWidgets`/`buildTeamLeadWidgets`/`buildEmployeeWidgets`/`buildViewerWidgets` in `apps/api/src/modules/s6/routes.ts` (dashboard templates for super_admin/admin/hr_manager/project_manager/team_lead/employee/client_viewer), then traced `widget.link` into `apps/web/components/WidgetGrid.tsx`'s `<Link href={widget.link}>` ("View →") | Every "View →" link opens the matching web screen | Six of eight templates' widgets (all but `payroll_officer`/`inventory_manager`, which already linked to `/payroll`/`/inventory`/`/assets`) linked straight at a bare JSON API route — `/api/v1/tasks`, `/api/v1/employees`, `/api/v1/attendance/records`, `/api/v1/leave/requests`, `/api/v1/notifications?unread=true`, `/api/v1/projects?status=ACTIVE`. Web auth is bearer-token-via-`fetch` only (`apps/web/lib/apiClient.ts`), not cookie-based, and `AppLink`/`staticHref` (`apps/web/lib/routes.ts`) never rewrites `/api/*` paths — so a plain `<Link>` navigation sent the browser to that path with no `Authorization` header, landing on a bare `401 UNAUTHENTICATED` JSON page instead of the dashboard drill-down, for nearly every widget on the landing page of nearly every role | FIXED — remapped each `widget()` call's link to the corresponding web page (`/my-work`, `/employees`, `/attendance`, `/leave`, `/inbox`, `/projects`), matching the pattern the two already-correct templates used. RED: new test in `apps/api/test/s6.test.ts` ("never links a widget straight at a bare JSON API route (B-022)") asserted across all 9 role templates that no `widget.link` starts with `/api/`; failed pre-fix (`expected true to be false`). GREEN: `apps/api/test/s6.test.ts` 32/32, `apps/web/tests/s6.test.ts` 18/18 | bf18bb6 |
