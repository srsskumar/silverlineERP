# Findings ledger — Lane B (dashboard, projects, planning, my-work, automation, analytics, reports, leads/clients/tenders, billing/receivables/payables/expenses/ledgers, procurement, inventory/stock, assets/asset-movements, documents, approvals, survey, record)

Companion to `docs/qa/2026-09-24/coverage.md`. Lane A's ledger is `findings-a.md` (kept separate so
the two lanes never conflict on the same file; merge into one ledger at the end of the sweep).

| ID | Sev(P0–P3/DECISION/KNOWN-TLS) | Surface(web/mobile/api) | Module | Steps | Expected | Actual | Status | Fix commit |
|---|---|---|---|---|---|---|---|---|
| B-001 | P2 | api | automation | GET /automation-rules/:id | readable with automation.read | requires automation.manage | OPEN | |
| B-002 | P2 | api/web | billing/payables | Approve a payment run via `POST /api/v1/payment-runs/:id/decision` (`paymentrun.approve`), then look for a way to mark it disbursed/paid — checked `apps/web/app/payables/page.tsx` (line 497 talks about "releasing" but no button follows it) and `apps/api/src/modules/ledgers/routes.ts` (only `paymentrun.manage` build + `paymentrun.approve` decide routes exist) | An execute/disburse action moves an approved run to "paid" | No such route or UI exists on any surface; approved runs have no further lifecycle step | OPEN — known gap, `open-decisions-2026-09-22.md` | |
| B-003 | P2 | api/web | expenses | Submit an expense claim and look for a receipt/file attachment control — checked `apps/web/app/expenses/page.tsx` (only prose at line 814 mentions receipts) and `apps/api/src/modules/expenses/routes.ts` (no multipart/attachment route on `expense-claims`) | A file input lets the claimant attach a receipt image/PDF before or after submit | No file input anywhere in the claim form; no attachment endpoint server-side | OPEN — known gap, `open-decisions-2026-09-22.md` | |
| B-004 | P2 | api/web/mobile | procurement | Search the repo case-sensitively for `vendor.invoice`, `vendor_invoice`, `VendorInvoice`, `msme` across `apps/api/src/modules` and `apps/web/app` | Some vendor-invoice-lines + MSME data model backs the three-way PO/GRN/invoice match | Zero hits anywhere — the feature has no data model at all, not just a missing screen; three-way match is unreachable | OPEN — known gap, `open-decisions-2026-09-22.md` | |
| B-005 | P3 | web | procurement | Look for a requisition/PO/RFQ/GRN creation control in `apps/web/app/procurement/page.tsx` (755 lines) | A "New requisition"/"New PO"/"New RFQ"/"New GRN" form, matching the API's `requisition.manage`/`po.manage`/`rfq.manage`/`grn.manage` write routes | Zero creation controls found (`grep -n "MutationForm\|New requisition\|New order\|New RFQ\|Create"` → no matches); web is status-move/approve only | OPEN — narrower than assumed: mobile already covers requisition creation, see B-006 | |
| B-006 | INFO | mobile | procurement | Compare `apps/mobile/app/procurement.tsx:172-255` ("Raise" button → `submitNewRequisition`, gated `requisition.manage`) against the open-decisions assumption that requisition creation is missing on both surfaces | open-decisions-2026-09-22.md to be corrected — requisition creation exists on mobile | Confirmed present and correctly gated; PO/RFQ/GRN creation still missing on both web and mobile | OPEN — documentation correction, not a code gap | |
| B-007 | P2 | web/mobile | billing | Look for "New advance" / "New RA-bill" creation controls in `apps/web/app/billing/page.tsx` (1043 lines) and the mobile `project-finance.tsx` equivalent | A form to raise a fresh RA-bill or advance, matching `POST /api/v1/advances` and `POST /api/v1/ra-bills` (`apps/api/src/modules/billing/routes.ts:134,188`) | No such control on web (zero `MutationForm`/"New advance"/"New RA bill" hits); mobile not yet confirmed by this pass | OPEN — known gap, `open-decisions-2026-09-22.md` | |
| B-008 | P2 | web/mobile | inventory/stock | Look for any UI reachable from web or mobile for `stock-locations`, `stock-counts`, `stock-reservations`, `stock/reorder` (`apps/api/src/modules/stock/routes.ts`) | Multi-location stock, physical counts and reservations are manageable from at least one client | Zero references anywhere under `apps/web/app`, `apps/web/components`, or `apps/mobile/app` — the entire `stock` module (as opposed to the older single-location `inventory` module, which does have web CRUD) is API-only | OPEN — known gap, `open-decisions-2026-09-22.md`, broader than "creation screens" | |
| B-009 | P3 | mobile | documents | Look for a create/renew/legal-hold/delete action in `apps/mobile/app/documents.tsx` (225 lines) | At least renewal, matching web's `document.manage`-gated "New document"/"Renew" buttons | No create/renew action found — mobile is tab-switch + row-select only (read-only) | OPEN — newly found, not in open-decisions | |
| B-010 | P2 | mobile | planning | Look for cycle create/start/close, SLA config, workflow editor, custom-field, or bulk-task-move actions in `apps/mobile/app/planning.tsx` (175 lines) | At least one of the five web-side write actions (`cycle.manage`, `project.update`, `board.manage`, `custom_field.manage`, `task.update`) present on mobile | None found — mobile Planning renders `CycleGroups`/`CycleRow` only, no reference to any of the five permissions in the file | OPEN — newly found, not in open-decisions | |
| B-011 | P3 | mobile | leads/clients/tenders | Look for "New lead"/"Add a client"/"New tender" creation forms in `apps/mobile/app/pipeline.tsx`, `clients.tsx`, `tenders.tsx` | At least stage-transition parity (confirmed present in pipeline.tsx) plus some create path | Zero create forms on any of the three screens; clients.tsx and tenders.tsx are fully read-only (no write-permission reference at all in either file) | OPEN — newly found, not in open-decisions | |
| B-012 | P2 | api | reports | Call `POST /api/v1/reports` or `GET /api/v1/reports/:id/download` as a user with `report.generate` but missing the underlying per-type data permission (e.g. a payroll report without `payroll.read`) | 403 with the documented message ("needs both the report.generate permission and permission to read the data it covers") — `apps/api/src/modules/s6/routes.ts:758,882`; both routes' `preHandler` is bare `authenticate`, the real check is inline | Not yet exercised — flagged from static reading; a lane walker must call both routes on web and mobile to confirm parity | OPEN | |
